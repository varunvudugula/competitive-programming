#include<bits\stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,input;
        cin>>n;
        vector<int> s;
        for(int i=0;i<n;i++)
        {
            cin>>input;
            s.push_back(input);
        }
    }
    return 0;
}
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    while(t--)
    {
        int n,input;
        cin>>n;
        vector<int> m;
        for(int i=0;i<n;i++)
        {
            cin>>input;
            m.push_back(input);
            
        }
    }
}